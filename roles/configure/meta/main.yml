---
galaxy_info:
  author: Steve Fulmer
  description: Configure HAProxy from structured variables
  license: GPL-3.0-or-later
  company: Red Hat
  min_ansible_version: "2.16"
  platforms:
    - name: EL
      versions: ["8", "9"]
    - name: Ubuntu
      versions: [jammy, noble]
    - name: Debian
      versions: [bookworm]
  galaxy_tags:
    - haproxy
    - loadbalancer
    - networking

dependencies:
  - role: sfulmer.haproxy.install
